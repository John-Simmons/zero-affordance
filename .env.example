# Copy to `.env.local` and fill in to use the Supabase backend.
# When these are absent, the app automatically uses the local mock provider,
# so you can develop with zero backend setup.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
